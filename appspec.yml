version: 0.0
os: linux

files:
  - source: target/demo-0.0.1-SNAPSHOT.jar
    destination: /home/ubuntu/app

hooks:
  ApplicationStart:
    - location: scripts/start.sh
      timeout: 300
      runas: ubuntu